<Page x:Class="Deli_Counter.XAML.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Deli_Counter.XAML"
      xmlns:controls="clr-namespace:Deli_Counter.Controls"
      mc:Ignorable="d"
      Title="HomePage">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Text="Current Install" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="0" Grid.Row="0" />
        <StackPanel Grid.Column="1" Grid.Row="0" >
            <TextBlock Text="Repository Status" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="0" Grid.Row="0" />
            <controls:ModRepository />
        </StackPanel>

    </Grid>
</Page>