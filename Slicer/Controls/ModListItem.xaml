<UserControl x:Class="Slicer.Controls.ModListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="75" d:DesignWidth="600"
             Height="75">
    <DockPanel>
        <Image x:Name="ModImage" Source="/Assets/placeholder.jpg" Margin="0 2 2 2" Stretch="Uniform"
               DockPanel.Dock="Left" />
        <DockPanel Margin="2 0 0 0">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4">
                <TextBlock x:Name="LocalStatusIcon" FontFamily="Segoe MDL2 Assets" Text="&#xE8FB;" FontSize="20"
                           FontWeight="UltraBold" Foreground="Green" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top">
                <TextBlock x:Name="ModTitle" Text="Placeholder Mod name"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock x:Name="ModShortDescription" Text="Short description of the mod." TextWrapping="Wrap"
                           Style="{StaticResource BaseTextBlockStyle}" />
            </StackPanel>
        </DockPanel>
    </DockPanel>
</UserControl>