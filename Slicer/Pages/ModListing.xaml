<UserControl x:Class="Slicer.Pages.ModListing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:controls="clr-namespace:Slicer.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer>
        <DockPanel>
            <TextBlock x:Name="CategoryTitle" Text="Category Name" Style="{StaticResource HeaderTextBlockStyle}"
                       DockPanel.Dock="Top" />
            <TextBlock x:Name="CategoryDescription"
                       Text="This is the sample category description for stuff and things!"
                       Style="{StaticResource BaseTextBlockStyle}" DockPanel.Dock="Top" Padding="0 0 0 8" />
            <ui:ListView DockPanel.Dock="Bottom"
                         IsSelectionEnabled="False"
                         x:Name="ModList"
                         BorderThickness="1"
                         BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
                         SelectionChanged="ModList_SelectionChanged" />
        </DockPanel>
    </ScrollViewer>
</UserControl>