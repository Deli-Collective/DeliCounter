<ui:Page x:Class="Slicer.Pages.HomePage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:local="clr-namespace:Slicer.Pages"
         xmlns:ui="http://schemas.modernwpf.com/2019"
         xmlns:controls="clr-namespace:Slicer.Controls"
         mc:Ignorable="d"
         d:DesignHeight="450" d:DesignWidth="800"
         Title="HomePage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Text="Current Install" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="0" Grid.Row="0" />
        <StackPanel Grid.Column="1" Grid.Row="0">
            <TextBlock Text="Repository Status" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="0"
                       Grid.Row="0" />
            <controls:ModRepositoryStatus x:Name="ModRepo" />
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBlock Text="Slicer Build Info" Style="{StaticResource HeaderTextBlockStyle}" />
            <controls:ApplicationGitInfo />
        </StackPanel>

    </Grid>
</ui:Page>